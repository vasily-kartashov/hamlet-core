language: php
sudo: false

php:
  - '7.0'
  - '7.1'
  - '7.2'
  - '7.3'
  - '7.4'
  - nightly

before_script:
  - composer update

script:
  - ./vendor/bin/phpunit --testdox
  - ./vendor/bin/phpstan analyse --level 7 src -c phpstan.neon
  - ./vendor/bin/psalm
  - ./vendor/bin/phpcs --standard=PSR2 -n src
